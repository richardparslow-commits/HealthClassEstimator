name: Deploy Streamlit app

# On every push to the main branch (and on manual dispatch), run the engine
# test suite, build the self-contained single-file HTML, and push it — plus the
# current index/css/js assets — into the richardparslow-commits/health_app.py
# repo. Streamlit Community Cloud watches that repo and redeploys on push, so
# the push IS the deploy trigger.
#
# Required setup: create a personal access token with "Contents: read/write"
# scope on richardparslow-commits/health_app.py and add it to this repo's
# secrets as STREAMLIT_PUSH_TOKEN. Until that secret exists, the workflow runs
# the test + build steps and skips the push with a warning.
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-streamlit
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out HealthClassEstimator
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run engine test suite
        run: npm test

      - name: Build single-file embedded HTML
        run: node scripts/build-embedded.js app_embedded.html

      - name: Check out streamlit repo (health_app.py)
        if: ${{ secrets.STREAMLIT_PUSH_TOKEN != '' }}
        uses: actions/checkout@v4
        with:
          repository: richardparslow-commits/health_app.py
          token: ${{ secrets.STREAMLIT_PUSH_TOKEN }}
          path: streamlit-app

      - name: Sync built app and assets into streamlit repo
        if: ${{ secrets.STREAMLIT_PUSH_TOKEN != '' }}
        run: |
          cp app_embedded.html streamlit-app/
          cp index.html css/styles.css streamlit-app/
          mkdir -p streamlit-app/js
          cp js/rules.js js/engine.js js/app.js streamlit-app/js/

      - name: Commit and push (triggers Streamlit Cloud redeploy)
        if: ${{ secrets.STREAMLIT_PUSH_TOKEN != '' }}
        env:
          SOURCE_SHA: ${{ github.sha }}
        run: |
          cd streamlit-app
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to deploy — streamlit repo already current."
          else
            git commit -m "Sync from HealthClassEstimator ${SOURCE_SHA:0:7}"
            git push
          fi

      - name: Warn when the deploy token is not configured
        if: ${{ secrets.STREAMLIT_PUSH_TOKEN == '' }}
        run: |
          echo "::warning::STREAMLIT_PUSH_TOKEN secret is not set. The engine suite ran and the single-file HTML was built, but the streamlit repo was not updated. Create a PAT with Contents read/write on richardparslow-commits/health_app.py and add it as STREAMLIT_PUSH_TOKEN in this repo's secrets, then re-run this workflow."
